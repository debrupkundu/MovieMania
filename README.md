# MovieMania
MovieMania is a React-based movie search and watchlist app powered by the OMDb API. Users can search movies, view detailed info, rate them with stars, and manage a watched list stored in localStorage. Built using React hooks, API fetching, and clean component-based UI.
